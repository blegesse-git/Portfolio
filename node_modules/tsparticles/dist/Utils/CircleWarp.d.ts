import { Range } from "tsparticles/dist/Utils/Range";
import type { ICoordinates } from "tsparticles/dist/Core/Interfaces/ICoordinates";
import { Circle } from "tsparticles/dist/Utils/Circle";
import type { IDimension } from "tsparticles/dist/Core/Interfaces/IDimension";
export declare class CircleWarp extends Circle {
    private readonly canvasSize;
    constructor(x: number, y: number, radius: number, canvasSize: IDimension);
    contains(point: ICoordinates): boolean;
    intersects(range: Range): boolean;
}
