import type { Particle } from "tsparticles/dist/Core/Particle";
import type { Range } from "tsparticles/dist/Utils/Range";
import type { Point } from "tsparticles/dist/Utils/Point";
import { Rectangle } from "tsparticles/dist/Utils/Rectangle";
import type { ICoordinates } from "tsparticles/dist/Core/Interfaces/ICoordinates";
import type { Container } from "tsparticles/dist/Core/Container";
import type { IDimension } from "tsparticles/dist/Core/Interfaces/IDimension";
export declare class QuadTree {
    readonly rectangle: Rectangle;
    readonly capacity: number;
    readonly points: Point[];
    private northEast?;
    private northWest?;
    private southEast?;
    private southWest?;
    private divided;
    constructor(rectangle: Rectangle, capacity: number);
    subdivide(): void;
    insert(point: Point): boolean;
    queryCircle(position: ICoordinates, radius: number): Particle[];
    queryCircleWarp(position: ICoordinates, radius: number, containerOrSize: Container | IDimension): Particle[];
    queryRectangle(position: ICoordinates, size: IDimension): Particle[];
    query(range: Range, found?: Particle[]): Particle[];
}
