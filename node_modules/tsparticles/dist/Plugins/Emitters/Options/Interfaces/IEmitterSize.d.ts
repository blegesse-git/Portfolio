import type { SizeMode } from "tsparticles/dist/Enums";
import type { IDimension } from "tsparticles/dist/Core/Interfaces/IDimension";
export interface IEmitterSize extends IDimension {
    mode: SizeMode | keyof typeof SizeMode;
}
